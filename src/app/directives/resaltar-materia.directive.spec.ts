import { ResaltarMateriaDirective } from './resaltar-materia.directive';

describe('ResaltarMateriaDirective', () => {
  it('should create an instance', () => {
    const directive = new ResaltarMateriaDirective();
    expect(directive).toBeTruthy();
  });
});
